@model IEnumerable<GrygierSite.Models.Product>
@{
    ViewBag.Title = "Home Page";
}

<ul>
    @foreach (var product in Model)
    {
        <li>
            @product.Name
        </li>
        <li>
            <img src="@product.ThumbnailPath"/>
        </li>
    }
</ul>